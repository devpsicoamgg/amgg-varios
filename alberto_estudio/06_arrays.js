// 


function sumNumbers (array) { 
//Crea un arreglo de números y calcula la suma de todos los elementos.
var sum = 0;
for (var i = 0; i < array.length; i++) {
    sum = sum + array[i];
}
return sum
}
//-------------------------------------------------------------------------------------------------------------------
function arrayConcat (arr) { 
    //Dado un arreglo de strings, conviértelo en un solo string concatenando todos los elementos.
    var newArray = arr.join("");
    return newArray;
    }
//Crea un arreglo de objetos, donde cada objeto tenga una propiedad "nombre" y otra propiedad "edad". Luego, filtra el arreglo para obtener solo los objetos cuya edad sea mayor o igual a 18.

//Dado un arreglo de números, ordena los elementos de manera ascendente y luego de manera descendente.

//Dado un arreglo de strings, crea un nuevo arreglo que contenga solo los elementos que empiezan con una vocal.

//Crea una función que reciba un arreglo de números y devuelva la media aritmética de todos los elementos.

//Dado un arreglo de objetos, ordena los elementos de manera ascendente según la propiedad "nombre".

//Crea una función que reciba un arreglo de números y devuelva el número más grande y el más pequeño.

//Dado un arreglo de strings, crea una nueva cadena que contenga la concatenación de todos los elementos separados por un guión.

//Crea una función que reciba dos arreglos y devuelva un nuevo arreglo que contenga la unión de ambos, sin elementos duplicados.